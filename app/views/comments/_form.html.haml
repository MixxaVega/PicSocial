-if user_signed_in?
	= form_for([@pic, @pic.comments.build]) do |f|
		.form-group
		%br
		= f.label "Comentario"
		%br
		= f.text_area :body, placeholder: "Máximo 250 caracteres o no podrá comentar."
		%br 
		%br
		= f.submit "Aceptar",class: "btn btn-info"
